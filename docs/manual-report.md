# technical-book audit

Manual: `docs/manual`
Standard: `~/.claude/skills/technical-book/reference/standard.md`
Config: `.agent/config.yaml`
Coverage: 138 of 138 documented (100%), threshold 100%
Build: `wcl wdoc build docs/manual/main.wcl` exit 0 (66 pages)
Run: 2026-09-02

## Findings

The script half reported no findings: every part, chapter, section, callout, diagram and listing rule passed, and coverage is complete.

| Finding | Severity | Status | Detail |
|---------|----------|--------|--------|

## Judgement

These are opinions, from the standard's closed list. They dismiss like any other finding. 186 findings over 60 of 66 chapters; clean chapters: Screens, input and vision, vmlab down, vmlab validate, vmlab ssh-config, vmlab tail, vmlab osinfo. By check: example-quality 70, prose-review 49, cross-reference 39, progressive-disclosure 14, mode-fit 7, callout-admission 6, title-honesty 1.

| Finding | Status | Detail |
|---------|--------|--------|
| `Introduction/cross-reference` | open | `intro.wcl:16,39` name QMP and `intro.wcl:41` virtio-serial with no expansion and no link. Link QMP and "agent" to `guide_architecture` or the glossary (`apx_glossary`) on first use. `intro.wcl:33` "config-weave plays with a drift check" is undefined jargon; say "declarative config steps with a drift check" and keep the `guide_playbooks` link. |
| `Introduction/prose-review` | open | Wrong fact about the manual itself: `intro.wcl:82` says Reference defines "the events, the files vmlab writes, and the wire protocol", but `main.wcl` puts those three under Appendices (`ref_events`, `ref_paths`, `ref_protocol`), and `intro.wcl:84` lists Appendices without them. Move the three to the Appendices bullet. `intro.wcl:39` "exactly two doors" contradicts `first-vm.wcl:166` "three doors into the guest"; pick one count or drop the metaphor. `intro.wcl:31` packs two links and three concepts into one sentence; split. |
| `Install/progressive-disclosure` | open | The package table and asset section use lab-file fields before any lab file has been shown: `firmware = "ovmf"` (`install.wcl:97`), `tpm = true` (:98), `media {}` (:100), `gui = true` (:109), `agent = false` (:187), `wait_for_text` (:102). Either describe in plain words ("guests that declare a TPM") or link each to `ref_vm`/`ref_template`/`ref_ws_machine`. |
| `Install/example-quality` | open | `install.wcl:172-174` lists the host tools `build-asset.sh` needs but omits `awk`, `install` and `find`, which `guest/build-asset.sh:326` checks for. `install.wcl:117-118` "`vmlab validate` does not probe the host" is true for tools, but validate does consult the template store (`src/cli/validate.rs:14`) and writes `~/.ssh/config` (`src/cli/validate.rs:123`); say "does not check for tools". |
| `Install/cross-reference` | open | `vmlab validate`, `vmlab up`, `vmlab console` (`install.wcl:118-120`) are not linked to `cmd_validate`, `cmd_up`, `cmd_console`; "network fabric" (:99, :198) not linked to `guide_networking`; `media {}` (:100) not linked to `ref_vm`. |
| `Install/prose-review` | open | `install.wcl:106` "Two more tools are optional" then "`sqfstar` is required the first time you run a container": contradiction; say "Two tools are needed only for some features". `install.wcl:109-111` semicolon chain and passive "is preferred"; split into two sentences with vmlab as the actor. `install.wcl:200` "makes the Windows kernel a non-issue" is colloquial; "means nothing on the Windows side needs configuring". `install.wcl:111-113` sentence over 30 words; split at "and fall back". |
| `Your first lab/example-quality` | open | `first-lab.wcl:90-91` "It starts nothing and writes nothing" is wrong: `vmlab validate` writes the managed block into `~/.ssh/config` (`src/cli/validate.rs:123`), confirmed by running it under an isolated home, and `dev-attach.wcl:123` says so. Change to "starts nothing" and mention the ssh config block, or drop "writes nothing". |
| `Your first lab/progressive-disclosure` | open | The destroy warning (`first-lab.wcl:236`) names `share {}`, `volume {}` and `vmlab cp`, none introduced yet and none linked. Link to `guide_shares`, `guide_containers` and `cmd_cp`. `first-lab.wcl:75` names a `forward {}` block without a link (`ref_lab`). |
| `Your first lab/cross-reference` | open | Next steps link only `cmd_up` and `cmd_container`; the chapter also teaches `validate`, `status`, `exec`, `down`, `destroy` and `lab list` with no links to `cmd_validate`, `cmd_status`, `cmd_exec`, `cmd_down`, `cmd_destroy`, `cmd_lab`. |
| `Your first lab/prose-review` | open | `first-lab.wcl:140` second sentence has no verb ("The container's address on its own, and the log of its console..."); make it "One prints the container's address, the other the console log...". `first-lab.wcl:98-101` carries three instructions in one sentence (misspell, run, put back); split. `first-lab.wcl:163-165` colon chain plus rule of three; "so nothing on the host was configured. There is no iptables rule and no bridge." `first-lab.wcl:218-219` "no healthcheck history to wait for beyond the first pass" is unclear; say what actually makes it faster (the cached image). |
| `Your first VM/callout-admission` | open | `first-vm.wcl:211-214` "Snapshots are not a workspace backup" is `:note` here and `:warning` in `dev-attach.wcl:277`. It describes work loss, and the product prints it on every snapshot verb; make it `:warning` or drop it here, where no dev machine exists. |
| `Your first VM/progressive-disclosure` | open | `first-vm.wcl:213` uses "workspace" and "re-converged" before either is taught (they arrive in `gs_dev_attach`). If the callout stays, phrase it as "on a dev machine the synced directory is restored from the host, not from the snapshot". `first-vm.wcl:127` "your host configuration" not linked (`guide_host`). |
| `Your first VM/cross-reference` | open | `vmlab pull` (`first-vm.wcl:85`), `vmlab template list`/`rm` (:90, :231), `vmlab exec`/`shell` (:108, :120), `vmlab status` (:89) not linked to `cmd_pull`, `cmd_template`, `cmd_exec`, `cmd_shell`, `cmd_status`. |
| `Your first VM/prose-review` | open | `first-vm.wcl:166` "three doors" vs intro's "two doors" (see Introduction). `first-vm.wcl:145` and `:176-177` use semicolons as connectors; split. `first-vm.wcl:24` "a few tens of seconds" is a hedge; give a size or drop. `first-vm.wcl:190-193` packs the `--vm` flag, lab-wide behaviour and the consistency caveat into one paragraph; move the consistency caveat to its own sentence. |
| `Build a template from an ISO/progressive-disclosure` | open | The chapter reads `install.ws` (`first-template.wcl:61-65`) with `match`, `Ok`/`Err`, `?` and `Result[unit, string]`, none explained, while the next chapter (`first-provision.wcl:40-47`) is where the shape of a script is taught. Either add one paragraph on `main`, `Result` and `?` before the listing, or link `guide_scripting` at `:59`. `first-template.wcl:142,148` introduce `forward {}` and `nic { ip = ... }` without explanation or link (`ref_lab`, `ref_vm`). |
| `Build a template from an ISO/cross-reference` | open | `wait_for_text`, `send_keys`, `type_text`, `wait_shutdown` (`:68-69`) not linked to `ref_ws_machine`; "artefact cache" (:54, :102) not linked to `guide_templates`/`ref_paths`; `profile = "linux-modern"` (:53) not linked to `guide_profiles`; `gui = true` (:94) not linked to `ref_template`. |
| `Build a template from an ISO/prose-review` | open | `first-template.wcl:187-188` "It only prints what it would remove until you add `--yes`" misplaces "only"; "it prints only what it would remove". `first-template.wcl:20-23` passive and 35 words ("is packed into", "answers ... by reading the screen"); make the build the actor and split. `first-template.wcl:53` bullet is four sentences; move "The hardware is recorded..." to the paragraph after the list. |
| `Automate a guest with a script/cross-reference` | open | `lab.log` (:71, :145) not linked to `ref_ws_lab`; `.vmlab/` (:102, :156) not linked to `ref_paths`; `exec_timeout`, `copy_from` (:100-101) not linked to `ref_ws_machine` at first use. |
| `Automate a guest with a script/prose-review` | open | `first-provision.wcl:46` is 38 words with "but ... too, where"; split after "during `up`". `first-provision.wcl:145-149` runs four thoughts together (reruns, keep safe, copying harmless, account guard); split at "so keep scripts safe to repeat." and at "Creating an account twice". `first-provision.wcl:23` "would fail there by naming the missing capability" is awkward; "fails on a container with an error naming the missing display". `first-provision.wcl:100` semicolon; split. `first-provision.wcl:43` credits the file with `vmlab::sleep_ms`, which the listed `setup.ws` never calls; name a helper it does use or say "such as". |
| `Attach an editor to a dev machine/example-quality` | open | `dev-attach.wcl:313-316` says the alias leaves `~/.ssh/config` "the next time a command loads a lab and finds this directory no longer has a `vmlab.wcl`". Wrong at this commit: `vmlab destroy` withdraws the lab's aliases itself before tearing anything down (`src/cli/lab.rs:260-265`). Say destroy removes the stanza. `dev-attach.wcl:229-234` reads the other side immediately after each write (`cat /src/hello.lua`, `cat workspace/out.txt`) although `:238` says a per-path debounce applies; insert `vmlab dev sync flush` (introduced at `:264`) or a "wait a second" before each read, or move the flush section above this one. |
| `Attach an editor to a dev machine/cross-reference` | open | `vmlab machine capabilities`/`repair-agent` (:143-144) not linked to `cmd_machine`; `vmlab container destroy` (:291) not linked to `cmd_container`; `vmlab snapshot create` (:279) not linked to `cmd_snapshot`; `sftp`/facade (:188) not linked to `guide_logins`. |
| `Attach an editor to a dev machine/callout-admission` | open | `dev-attach.wcl:121-124` is a `:success` callout whose second sentence is a background fact (validate writes `~/.ssh/config`). Keep the success line and move the fact into a `:note` or the paragraph above. |
| `Attach an editor to a dev machine/prose-review` | open | `dev-attach.wcl:131` "cold-to-editing in one command" and `:244` "the proving case" are invented jargon; "brings you from a stopped machine to an editor in one command" and "the clearest example". `dev-attach.wcl:240-245` is one 50-word sentence with a colon chain and a `!` negation aside; split into three sentences. `dev-attach.wcl:285` "two existing verbs are one" is unclear; "because `destroy` then `up` is the rebuild". `dev-attach.wcl:299-301` "the latter" points across a three-item list; name it ("redo the hand-installs"). `dev-attach.wcl:32` semicolon; split. |
| `How vmlab runs a lab/progressive-disclosure` | open | L170 note explains the invariant by "why a halted workspace can only be resolved from the host" and "why the SSH facade refuses `ssh -R`"; neither the halt nor the facade is introduced until guide_logins/guide_dev. Cut the callout to the invariant plus "a guest with no network is still driveable", or link both. L180 uses `depends_on` before lab-file.wcl L70 introduces it; link guide_lab_file. |
| `How vmlab runs a lab/cross-reference` | open | L68 links the `vmlab-<lab>-<machine>` alias scheme to guide_logins; it is defined in guide_dev ("The managed SSH config block", dev.wcl L138). Retarget. L102 "workspace sync ledger" and "`dev use` selection" unlinked (guide_dev). L55 `lab.daemon_crashed` unlinked (guide_events). |
| `How vmlab runs a lab/prose-review` | open | L11 em dashes: "You declare a **lab**, the machines and the virtual networks between them, in one file". L112-115 ungrammatical and hides the path (src/paths.rs:77): "Some WSL setups leave `XDG_RUNTIME_DIR` unset. vmlab then falls back to `/tmp/vmlab-<uid>` and refuses one owned by anyone else." L189-190 stop-ladder sentence needs a colon after "machine" or a split. L22-23 says "thin client" twice; keep the second. |
| `The lab file/example-quality` | open | L33-37 quotes all ~100 lines of examples/mixed-lab/vmlab.wcl (share, forward, healthcheck, port, container) to illustrate "the import and the `lab` block". The file has anchors (`segment-lan`, `vm-winsrv`, `container-web`); use `anchor = "vm-winsrv"` or a hand-written 10-line lab. |
| `The lab file/progressive-disclosure` | open | L114 lists the dev-machine rules (`@dev(default = true)`, Windows `login` without `password`, `elevated`) before `@dev` is introduced at L151 and before guide_logins. Link `@dev` to the Decorators section and `login` to guide_logins, or move the row below L151. |
| `The lab file/cross-reference` | open | Table rows L55 (`record`, `sinkhole`) and L58 (`disk`, `media`, `gpu`) are the only unlinked rows; link guide_networking and guide_templates ("Media built from folders"). L146 "shipped `container` profile" unlinked (guide_profiles). L105 "registry reference" unlinked (guide_registries). |
| `The lab file/prose-review` | open | L44 "rule of thumb" idiom: "The rule is". L147-148 "One resolver implements this precedence… no other surface reimplements it" is an implementation note the reader cannot act on; cut. L88-91 "stops before any side effect on an error": move "on an error" to before "stops". |
| `Templates and the store/cross-reference` | open | L75-77 bolded "provision scripts and playbooks" unlinked (guide_scripting, guide_playbooks). L164 `host.disk_low` unlinked (guide_events). L194 `vmlab pull` unlinked (cmd_pull). |
| `Templates and the store/prose-review` | open | L89-92 list items use " — " separators; use a colon. L128 and L134 semicolons; split. L54 "Since the supervisor became the store's owner" is project history: "The supervisor owns the store, so…". L96-98 "on the hardware the block declares over the source template's recorded hardware" noun string; rephrase as "the block's hardware fields win over the source template's recorded ones". L100-103 six-step flow in a comma list; number it. |
| `Distributing templates over registries/cross-reference` | open | L114 "progress events you can watch" unlinked (guide_events). L120 callout `vmlab pull` unlinked (cmd_pull). |
| `Distributing templates over registries/prose-review` | open | L46-50 "binding constraint… wide margin" names no number; src/oci/chunking.rs:21 gives ten minutes: "GHCR times out an upload after ten minutes; a 512 MiB chunk uploads well inside that and retries cheaply." L26-28 quotes a docker error string vmlab does not control; drop the quotation marks. L133 "simply"; cut. |
| `Networking/callout-admission` | open | L234-237 "Declare `connect` on one side" is `tip` but describes a topology where dedup fails and two trunks form: a constraint the reader hits, so `note` (or `warning`). |
| `Networking/cross-reference` | open | L37 "its shares" and L83 "SMB services" name shared folders with no link to guide_shares. |
| `Networking/prose-review` | open | L90-91 claims the short `<machine>.<suffix>` alias is registered only "where that is unambiguous within the segment"; src/labd/network.rs:364-365 registers both names unconditionally. Drop the qualifier. L23-27 40-word sentence; split after "netdev". L171-172 colon connector; two sentences. Passives L61, L143: name the daemon/plan. |
| `Lab containers/example-quality` | open | L22-26 quotes the whole mixed-lab file (already quoted verbatim in lab-file.wcl L33-37) when the container block is 60 lines in; the file has `# ANCHOR: container-web`. Add `anchor = "container-web"`. |
| `Lab containers/cross-reference` | open | L61 "resolves like a registry template" and L82-83 "same lock and stage-then-rename discipline as the template store" unlinked (guide_registries, guide_templates). |
| `Lab containers/prose-review` | open | Passives L92 "is pinned", L124-125 "is installed… reinstalled", L171: name cinit/the daemon. L157-159 "wedge" is invented jargon; "block". L111-121 one paragraph carries bind vs named, survival, transport and fallback; split at "Volumes attach". |
| `Shared folders/example-quality` | open | L20-25: `template = "x86_64/ubuntu-24.04"` with a share `guest = "D:\\data"`, a Windows drive letter on a Linux guest; the Linux mount step (L119-120) would run `mount … D:\data`. Use two Linux paths or make the second share a Windows VM. (`~` for `host` is accepted, src/labd/share_plan.rs:402.) |
| `Shared folders/progressive-disclosure` | open | L126-130 assumes the minted-logon mechanism from guide_logins (chapter 12); say in half a sentence that the agent creates a Windows logon per declared `login`, and link guide_logins on "mints". |
| `Shared folders/prose-review` | open | L96 "Samba is a documented host package" says nothing; name the package and link gs_install. L105-106 colon connector; cut or merge. L43-47 30-word sentence; split after "migration mode enabled". Passives L53-54, L101, L127: the daemon/agent is the actor. L155-159 restates dev.wcl L215-219; keep one and link. |
| `Snapshots/progressive-disclosure` | open | L92-139 uses syncer vocabulary as known: "flush" L95, "conflict halt" L99, "rescan or re-seed" L100, "ledger" L102/L136, "dirty set" L129, "stat-walk"/"watch discontinuity" L133-134, "named skip" L111. All are defined in guide_dev, five chapters later. Define each in one clause with a link, or keep only the two callouts and the escape flag here and leave the mechanics to guide_dev L456-476, which already restates them. |
| `Snapshots/cross-reference` | open | L99 "conflict halt", L102 "ledger", L133 "stat-walk" unlinked; only L82 links guide_dev. Link each first use. |
| `Snapshots/prose-review` | open | L123 "Each word matters." filler; cut. L108-110 two-clause rhetorical restatement; keep one. L129 "self-inflicted paths": "paths the syncer itself wrote". Passives L30-31, L37-39: "vmlab refuses", "the daemon writes". L157 callout tail after "host's current tree" is a 35-word restatement; cut. |
| `Guest automation with wscript/example-quality` | open | L254: `vmlab __wscripti [path]` does not exist. The verb is `vmlab wscripti [OUT]` ("Internal: write the wscript interface file", default `vmlab.wscripti`). Fix the name and drop "hidden". |
| `Guest automation with wscript/progressive-disclosure` | open | L166 and L169 lean on "a template's first-boot script" and "a machine's own first-boot provision"; templates.wcl L28 only names "an optional first-boot script". Add one sentence defining the template block's `first_boot` field and link ref_template. |
| `Guest automation with wscript/prose-review` | open | L14, L262 "surface" jargon; "API". L102-109 and L114-128 semicolon chains with a six-item inline group list; split. L165-175 packs this_vm, first-boot readiness and agent_answering into one paragraph; give first-boot readiness its own. L223-231 five-clause sentence on `send_line`/`send`/`expect`; one method per sentence. L196-199 states the gotcha but not the fix; add "so write `copy_to("editor-bits.ps1", …)`". |
| `Playbooks/cross-reference` | open | L11: config-weave is never defined, linked or given a URL anywhere earlier in the guide (the glossary only says "a config-weave playbook"). Add one sentence at first mention saying it is an external configuration tool, with its home. |
| `Playbooks/prose-review` | open | L85-88 four list items are each a three-to-five-sentence paragraph under a bold label; make "What a run does" four short h3s. L87 passive chain "are rendered… is parsed and attached": "vmlab renders… and attaches". L92-97 retry sentence carries cause, symptom and idempotence; three sentences. |
| `Logins and the SSH facade/example-quality` | open | L93 callout "`vmlab cp` is the exception: it still runs as the agent identity" is false at this commit: `machine.push_file` is served with `logon_for(m, None, None)` (src/labd/mod.rs:703, 263-270), which resolves to the machine's default login. It also contradicts L82, which lists `vmlab cp` as person-invoked. Delete the sentence. The stale text also lives in the CLI help (src/cli/mod.rs:227 "Still the agent identity…"); fix code-side. |
| `Logins and the SSH facade/progressive-disclosure` | open | L185-186 "the managed SSH config block", L120 "degrades the workspace in two named ways", L266-268 "the repair verb" and "attachable and the failure ladder" are all guide_dev concepts. Add a one-sentence gloss at L185 (vmlab writes a `vmlab-<lab>-<machine>` stanza into `~/.ssh/config`) and name `vmlab machine repair-agent` at L266, or move the SSH config block section into this chapter. |
| `Logins and the SSH facade/cross-reference` | open | L267 "the repair verb" unlinked and unnamed; link cmd_machine. |
| `Logins and the SSH facade/callout-admission` | open | L91 `warning` "A declared login changes what exec and shell can do" is a hard behaviour change the reader hits (commands start failing), not data loss; `error` per the rubric. |
| `Logins and the SSH facade/prose-review` | open | L106-121 one paragraph carries seven facts (logon mode, why sshd moved host-side, refused logon types, `LoadUserProfileW`, privileges, linked token, elevation default); split into three. L158 typo "are attached to the same way": "in the same way". L179-188 mixes ssh-proxy's contract with `vmlab ssh`'s; split at "`vmlab ssh` refreshes". L194-207 one paragraph, five topics; split. |
| `Dev machines and the workspace syncer/example-quality` | open | L43-48 the `buildbox` container declares `image = "mcr.microsoft.com/dotnet/sdk:9.0"` in default `:workload` mode, so the SDK entrypoint exits and cinit powers the micro-VM off (containers.wcl L134-141 says a dev container wants `mode = :idle`; the neovim example sets it). Add `mode = :idle`. |
| `Dev machines and the workspace syncer/cross-reference` | open | L111-115 symlink and inotify preconditions link only cmd_machine; L86-88 the three feature strings could link the agent section of guide_architecture. |
| `Dev machines and the workspace syncer/prose-review` | open | L137 "load-bearing" metaphor: "where the command needs the alias". L378 "Ten conflicts do not become a bigger hammer" and L369-370 "durability work, not authorship" name a feeling; cut the first, and for the second say "the host writes only through git, tooling and vmlab's restore". L151-157 70-word sentence listing five writer properties; list it. L120-131, L150-166, L240-253 lead with rejected designs ("was rejected on evidence", "was rejected for the default"); lead with what happens and keep the rejection to one clause. L228-232 restates logins.wcl L86-88 word for word; keep one and link. Semicolon-joined sentences at L26-34, L72-79, L133-148, L183-189; split. |
| `Guest OS profiles/cross-reference` | open | L38-40 "the `login {}` validation rules and the Windows preconditions of the workspace syncer" unlinked; link guide_logins and guide_dev. |
| `Host configuration and WSL 2/cross-reference` | open | L142 `nested = true` is the only lab-file field in the chapter with no link; link ref_vm. L127 `ControlPath` unlinked (guide_dev). |
| `Host configuration and WSL 2/prose-review` | open | L66-67 "location knobs with one code path behind them rather than switches" repeats dev.wcl L128-129 verbatim; say it once and link. L80 passive; "`vmlab console` and `gui = true` choose a viewer the same way". L108 "a full-privilege interface" abstract: "anyone who can connect to a lab socket can run scripts and read and write guest files". L119-122 one sentence carries the WSL claim and two mechanisms; split after "WSL 2". |
| `Events and handlers/example-quality` | open | L107-108 claims the mixed-lab crash handler "calls `machine.start()`… for a container". The example binds only `on "vm.crashed"` (examples/mixed-lab/vmlab.wcl:81) and scripts/on-crash.ws only screenshots; there is no container handler and no `start()`. Drop the claim or point at a handler that exists. |
| `Events and handlers/prose-review` | open | L140 "Everything is logged… under `~/.local/state/vmlab/labs/<lab>/`" contradicts L113, which puts `labd-<lab>.log` directly under `~/.local/state/vmlab/` (src/supervisor/mod.rs:253); say "the lab's logs" and name the daemon log's location once. L144-145 passive "The files vmlab appends to itself roll over": "vmlab rolls its own log files over at a fixed size", and give the cap from `MAX_LOG_BYTES` (src/logs.rs:28). L14 "Any unrecoverable error is an event before it is a failure" names a feeling; cut. L101-104 one sentence carries subscription, per-handler task, concurrency and shutdown wait; split at "Handlers run concurrently". |
| `Lab file: lab and segment/example-quality` | open | `lab-file-lab.wcl:30` "The first line of the file must be `import <vmlab.wcl>`". `src/config/mod.rs:77` only checks the import appears anywhere; shipped examples put comments first. Fix: "must contain `import <vmlab.wcl>` before any block". |
| `Lab file: lab and segment/example-quality` | open | `lab-file-lab.wcl:140` mtu default "9000 on a `nat` or `global` segment". `src/labd/network.rs:68-73` returns jumbo only when `nat`; cross-host global stays 1500 (comment at :57-61). Fix: "9000 on a `nat` segment, else 1500" (schema `@doc` at schema.wcl:110 is wrong the same way). |
| `Lab file: lab and segment/example-quality` | open | `lab-file-lab.wcl:312` block `port` "Requires `proto`". Nothing enforces it: `extract.rs:195-196` reads them independently, `net/rules.rs:407-427` matches a port-only rule on any L4 proto. Fix: drop the clause or say a port-only rule matches TCP and UDP. |
| `Lab file: lab and segment/example-quality` | open | `lab-file-lab.wcl:193-195` "The daemon's own resolver keeps answering ... either way". With `enabled = false` no zone is built (`labd/network.rs:319,355,402`). Fix: only `server` keeps the daemon resolver; `enabled = false` runs no DNS. |
| `Lab file: lab and segment/example-quality` | open | `lab-file-lab.wcl:359` "Validation rejects an empty pattern" (and :345 says sinkhole applies in `lab {}`). `validate.rs:97-101` checks segment sinkholes only; lab-level ones are unchecked. Fix: scope the sentence to segment sinkholes, or fix the validator. |
| `Lab file: template and source/example-quality` | open | `lab-file-template.wcl:16-17` "reads `./vmlab.wcl` unless `--file`". `template/cli.rs:363-369` walks up with `find_lab_root`. Fix: "the nearest `vmlab.wcl` up from the current directory". |
| `Lab file: template and source/example-quality` | open | `lab-file-template.wcl:66` `disk` default "from source". `template/build.rs:194` uses 20GiB when unset for `iso`/`scratch`; only `qcow2`/`template` copy the source size (:225-231). Fix the Default cell. |
| `Lab file: template and source/example-quality` | open | `lab-file-template.wcl:109-118` Validation list (incl. :114 "A `scratch` source requires `disk`") runs only via `validate()` on a LabFile. `vmlab template build` loads through `load_template_source` (`supervisor/templates.rs:174`) with no `validate(` call; a bare template file with `source "scratch" {}` and no `disk` builds at 20GiB. Fix: say the list is enforced by `vmlab validate` / lab verbs; a bare template file gets schema, `arch` and `source {}` checks only. |
| `Lab file: template and source/example-quality` | open | `lab-file-template.wcl:105-107` "the check is skipped and the build log says so". `build.rs:757-768` skips silently; no such log line. Fix: drop "and the build log says so". |
| `Lab file: vm and its children/example-quality` | open | `lab-file-vm.wcl:96` "`disk` on a store or registry template is an error". `validate.rs:650-660` rejects it only in the `TemplateRef::Store` arm; the Registry arm (:662-671) checks `arch` only. Fix: "on a store template". |
| `Lab file: vm and its children/example-quality` | open | `lab-file-vm.wcl:76` `nic {}` row "Shares need at least one." `validate.rs:193-197` exempts `transport = "virtiofs"`; contradicts the chapter's own :78 and :101. Fix: "SMB shares need at least one". |
| `Lab file: vm and its children/example-quality` | open | `lab-file-vm.wcl:27-33` synopsis pairs a store `template` with `disk = 64GiB`, which `validate.rs:650` rejects as written. Fix: drop `disk` from the synopsis or comment it as rejected with a store template. |
| `Lab file: vm and its children/example-quality` | open | `lab-file-vm.wcl:283` "Validation ... rejects `smb1 = true` with `transport = "virtiofs"`". Raised at extraction (`extract.rs:339-344`), not in `validate.rs`; still surfaces from `vmlab validate`. Low severity; say "the loader rejects" if the chapter distinguishes passes. |
| `Lab file: container and its children/example-quality` | open | `lab-file-container.wcl:83` image rule omits two enforced checks: repository path components must be lowercase letters, digits, `.`, `_`, `-` (`model.rs:874-879`, test `container_bad_image` validate.rs:2166) and a registry host is alphanumerics, `.`, `-`, `:`, `_` (`model.rs:852-859`). Fix: add both. |
| `Lab file: container and its children/example-quality` | open | `lab-file-container.wcl:141` volume name "cannot ... contain a slash". `extract.rs:729` also rejects a backslash. Fix: "a slash or backslash". |
| `Lab file: container and its children/example-quality` | open | `lab-file-container.wcl:190-196` `interval`/`timeout` "greater than zero". `schema.wcl:413-415` declares `@range(1)` (whole seconds, per schema.wcl:44-46); code enforces non-zero only (`extract.rs:788-798`), so `500ms` passes. Three-way disagreement; align chapter, schema and code. |
| `Host configuration file/example-quality` | open | `host-config.wcl:58` example sets `config_weave_bin_dir = "~/.local/share/..."`. The reader is a bare `PathBuf::from` (`config/block.rs:381-383`) and `resolve_bin_dir` uses it untouched (`labd/playbook.rs:37-45`); `~` is expanded for `ssh_config` only (`ssh_config/mod.rs:236,490-495`). Fix: absolute path in the example; state the `~` rule. |
| `Host configuration file/example-quality` | open | `host-config.wcl:76` default `~/.local/share/config-weave/bin`. Code: `$XDG_DATA_HOME/config-weave/bin` (`paths.rs:56-58`), and env `VMLAB_CONFIG_WEAVE_DIR` sits between field and default (`labd/playbook.rs:32,48-51`). Fix: state the XDG default and the env var. |
| `Host configuration file/example-quality` | open | `host-config.wcl:25-27` "takes effect on the next `vmlab up` after the supervisor restarts". Supervisor reads once at start (`supervisor/mod.rs:48,110-112`); labd re-reads at lab assembly (`labd/lab.rs:431,1742`) so per-lab fields need only the next `up`; `viewer` (`viewer.rs:40`) and `ssh_config` (`ssh_config/mod.rs:509`) are read by the CLI on every call. Fix: give the three cases. |
| `Host configuration file/example-quality` | open | `host-config.wcl:85` "`VMLAB_FASTPATH` selects the same values". `net/fastpath/mod.rs:176-186`: the env var overrides the field, and a bad value is logged and ignored rather than rejected. Fix accordingly. |
| `Host configuration file/example-quality` | open | `host-config.wcl:101-102` "falls back to userspace silently". Each skipped tier's reason is logged (`net/fastpath/mod.rs:138-145`) and returned by `vmlab fastpath` (:160-174). Fix: "falls back and records why; `vmlab fastpath` shows the reasons". |
| `Guest OS profile files/example-quality` | open | `profiles.wcl:49-51` and Default column :77-85 ("QEMU default"/"unset"). `qemu/resolve.rs` supplies vmlab floors: `machine`→`q35` (:263-270), `disk_bus`→`virtio` (:319), `nic_model`→`virtio-net-pci` (:320-322), `cpus`→2 (:301-305), `memory`→2GiB (:306-310), `secure_boot`/`tpm`→false (:196-198,314-318), `firmware`→`ovmf` on non-x86 (:186-193); only `display` is truly absent (`cmdline.rs:381`). Fix the Default cells and :49-51. |
| `Guest OS profile files/example-quality` | open | `profiles.wcl:94` "`nic_model` and `display` are passed to QEMU as written". `display` is mapped by `display_device_name` (`resolve.rs:222-240`: `qxl`→`qxl-vga`, `std`→`VGA`, `virtio-gpu`→`virtio-gpu-pci`); :83 example list omits the accepted `virtio-gpu` (`profile_schema.wcl:19`). Fix: state the mapping. |
| `Guest OS profile files/example-quality` | open | `profiles.wcl:110-111,123` "every Linux one at `/src`", container "nothing else applies". `shipped.wcl:127` sets `workspace_guest = "/src"` on `container`; `custom` (:130-132) sets none. Fix the sentence and the `container` row. |
| `Guest OS profile files/example-quality` | open | `profiles.wcl:100-101` "else the default" unnamed. Code resolves against `default_profile()` (`resolve.rs:163-170`), an anonymous profile with only `agent_channel = true`, not the shipped `custom`. Fix: name it. |
| `wscript API: Lab and Segment/example-quality` | open | `wscript-lab.wcl:283` `lab.log("wildcard rule " + id)` does not compile: `dns_set` returns `Result[int, string]` (`scripting/mod.rs:564`), E0220 string + int. Fix: `+ str(id)` (verified). |
| `wscript API: Lab and Segment/example-quality` | open | `wscript-lab.wcl:354` same defect in the `block` example (`mod.rs:585`). Fix: `+ str(id)`. |
| `wscript API: Lab and Segment/example-quality` | open | `wscript-lab.wcl:63-64` handler `lab.log` "lands in the lab log only". Handlers get a `tracing::info!` sink (`labd/mod.rs:164-166`) into `labd-<lab>.log` (`supervisor/mod.rs:253-268`); only provision/`vmlab script` runs write `lab.log` (`labd/mod.rs:213-233`). Fix the sentence. |
| `wscript API: Lab and Segment/example-quality` | open | `wscript-lab.wcl:324-325` lab-file records "have no id and cannot be cleared from a script". They share the same id counter (`labd/network.rs:369-382`, `net/dns.rs:184-218`); a script is just never told the id. Fix wording. |
| `wscript API: Lab and Segment/example-quality` | open | `wscript-lab.wcl:103-104` (also :173, :213) machine order "is the runtime's own". It is a `BTreeMap` by name (`labd/lab.rs:44,999`). Fix: "sorted by machine name". |
| `wscript API: Term, types and the vmlab module/example-quality` | open | `wscript-types.wcl:326-328` "the daemons set both keys on such events". `matching_handler_runs` reads `vm`/`container` only (`labd/mod.rs:54-57`) and `workspace.*` (`lab.rs:1675,1725,2099`, `workspace/syncer.rs:711`) and `playbook.*` (`labd/playbook.rs:443-487`) carry `machine` alone, so `event.vm` is empty there. Fix: name those families. |
| `wscript API: Term, types and the vmlab module/example-quality` | open | `wscript-types.wcl:305-313` the `Event` block pairs a `struct Event` with `fn handle`; pasted as a script it fails E0202 (host type already defined, `mod.rs:253-259`). Fix: drop the `fn handle` line or caption the struct blocks as the generated interface shape. |
| `wscript API: Machine/example-quality` | open | `wscript-machine.wcl:729` two `let`s on one line in the `find_image` example; wscript ends statements at newline/`;` (E0109, `vendor/wscript-compiler/src/parser.rs:1243-1265`). Does not compile. Fix: split or add `;`. |
| `wscript API: Machine/example-quality` | open | `wscript-machine.wcl:999` `copy_to(..., "%APPDATA%\\Code\\...")`: the agent opens the path verbatim (`guest/agent/src/fileops.rs:341,493`); nothing expands `%APPDATA%`. The shipped example computes the home path (`examples/dev-vscode-windows/scripts/editor-bits.ws:59,87`). Fix: explicit path, and say guest paths are not expanded. |
| `wscript API: Machine/example-quality` | open | `wscript-machine.wcl:1015-1017` `as_account` "fails when the guest rejects the pair". It resolves host-side only (`mod.rs:311-331,1038-1043`; `labd/identity.rs:60-79` has no failing branch with a password); errors surface on the first guest call. Fix accordingly. |
| `wscript API: Machine/example-quality` | open | `wscript-machine.wcl:434-435` `snapshots` "fails when the machine does not exist". `labd/lab.rs:2126-2139` always returns `Ok` (empty list). Fix. |
| `wscript API: Machine/example-quality` | open | `wscript-machine.wcl:991-993` `as_login` fails only on no match. `identity.rs:103-120` also refuses a declared Windows login with no password. Fix: add the second refusal. |
| `wscript API: Machine/example-quality` | open | `wscript-machine.wcl:218-221` first-boot `wait_ready` message is "<name>: not agent-answering after <timeout>" (`labd/machine.rs:396-400,716-735`), timeout in Duration form (`300s`). Fix the quoted text. |
| `Lab file: lab and segment/cross-reference` | open | `lab-file-lab.wcl:90` the `on {}` row is not linked to where the block is documented (currently `ref_vm`, see title-honesty below). `:317-318` "Scripts can add and remove rules" links `guide_scripting`; the entries are `Segment.block`/`redirect` in `ref_ws_lab`. |
| `Lab file: vm and its children/title-honesty` | open | `lab-file-vm.wcl:394-423` documents `on {}`, a `lab {}` child (its own :396 says so), under "vm and its children"; `ref_lab` has no `on {}` section. Move it to `ref_lab` and link from `ref_vm`. |
| `Lab file: vm and its children/mode-fit` | open | Explanation inside reference: `:116-118` why `@dev` is a decorator; `:183-184` justifications after each login rule; `:188-192` callout body argues the design; `:389-391` "because config-weave binds each override as a `let`". Keep the rules, link `guide_dev`/`guide_logins`/`guide_playbooks` for the why. |
| `Lab file: vm and its children/cross-reference` | open | `:82` first mention of config-weave with no link (Playbooks link only at :350); `:350` `vmlab playbook check`/`apply` unlinked (`cmd_playbook`). |
| `Lab file: container and its children/progressive-disclosure` | open | `:55` `mode` has type `symbol` (`:workload`), a WCL value type neither the "Value types" callout (`lab-file-lab.wcl:57-62`) nor `guide_lab_file` defines. Add `symbol` to the callout and point at it from this chapter. |
| `Lab file: container and its children/cross-reference` | open | `:25` `exec` and `cp` unlinked (`cmd_exec`, `cmd_cp`); `:174` `container.unhealthy` unlinked (`ref_events`); `:69` "the user cinit resolves" unlinked (`guide_containers`). |
| `Lab file: container and its children/mode-fit` | open | `:97` "since it has no service to be" and `:142-144` named-volume lifecycle narrative (survives `down`, `destroy` removes, snapshot scope) belong in `guide_containers`; keep the validation facts. |
| `Host configuration file/mode-fit` | open | `:90-96` two design rationales ("is a location, not a switch", "because the cap is about this developer's link ... not the lab everyone shares") are explanation; keep the facts, move the why to `guide_host`/`guide_dev`. |
| `Host configuration file/cross-reference` | open | `:70` `host.disk_low` unlinked (`ref_events`); `:75` `vmlab template push` unlinked (`cmd_template`); `:102` `vmlab fastpath` unlinked (`cmd_fastpath`). |
| `Guest OS profile files/mode-fit` | open | `:126-133` explains and instructs ("Its floor is an order of magnitude below a VM's on purpose ... Raise it per container with ..."); reference keeps "carries only `cpus` and `memory`" and links `guide_profiles`. |
| `Guest OS profile files/callout-admission` | open | `:134` "Overriding replaces, it does not merge" is `:warning`; the rule admits warning for losing work, data or money. This is a hard constraint the reader will otherwise hit: `:error`, or plain prose beside the load-order paragraph at :24-26 that already states it. |
| `Guest OS profile files/cross-reference` | open | `:88` `transport = "auto"` unlinked to the `share {}` entry (`ref_vm`); `:102-104` login family rule unlinked to `login {}` in `ref_vm`; `:131` per-container `cpus`/`memory` unlinked (`ref_container`). |
| `wscript API: Lab and Segment/mode-fit` | open | `:85-87` "Use this when ..." and `:128-130` "which tells you more than ... reads well" are advice/opinion; state the behaviour. |
| `wscript API: Lab and Segment/cross-reference` | open | `:24` `vmlab script` unlinked (`cmd_script`). |
| `wscript API: Machine/cross-reference` | open | `:239` `vmlab machine capabilities` unlinked (`cmd_machine`); `:408` `vmlab snapshot restore --discard-guest-changes` and `:435` `vmlab snapshot list` unlinked (`cmd_snapshot`); `:417` `vmlab dev sync resolve` unlinked (`cmd_dev`); `:986` `vmlab exec --user` unlinked (`cmd_exec`). |
| `wscript API: Term, types and the vmlab module/mode-fit` | open | `:37`, `:81`, `:107-109`, `:351-354` are instructions ("Use this for...", "Prefer the waiting methods..."); `:382-385` rationale for `env`. Keep the facts, move advice to `guide_scripting`. |
| `wscript API: Term, types and the vmlab module/cross-reference` | open | `:281` `vmlab machine capabilities` unlinked (`cmd_machine`); `:323` `on {}` links `guide_events` but the block's field reference is in `ref_vm` (currently) and should link there too. |
| `Lab file: lab and segment/prose-review` | open | (technical-writing review, skill invoked) L23-24 figurative "the way `git` finds its repository", cut; L52-54 three thoughts in one sentence, split; L67-68 "unique on the host" is not what `validate` checks (`validate.rs:1218-1233`), say where it is enforced or drop; L108-109 trailing "or ... when" reads two ways; L140 "9000 or 1500" is not a Default value, put the rule in Meaning; L163 explanation inside a validation bullet; L200-202 "both hosts set in their host config" dangles; L263-265 and L359-360 same caveat in two wordings; L300-301 "where it can" hedges (code: RST for TCP, ICMP unreachable otherwise, `rules.rs:10-11`); L315-317 "most specific wins" understates the tie-break order (`rules.rs:15-21`). |
| `Lab file: template and source/prose-review` | open | L22-25 five build steps in one sentence, split; L25 store path vs L90-91 `<version>.<N>` disagree, align; L61 and L112 repeat the uniqueness rule; L79 passive "Steps stream as ..."; L89-97 one 7-sentence block mixing rule, mechanism and CLI pointer, break up; L101-103 two "it"s with different referents; L126-128 duplicates L141; L149 duplicates L152; L151 "here" vague, say "as `from`"; L154-157 these are loader checks (`extract.rs:907-957`) but the chapter calls them "Validation", define the pass. |
| `Lab file: vm and its children/prose-review` | open | L11-17 passive "is enforced by"; L176-180 45-word sentence with colon, split into three; L225 and L227 40-plus-word rule sentences, split or sub-bullet; L282-283 non-parallel series ("requires `host` to be a directory, a derivable ... `name`, and rejects"); L308-310 "which stays shared" dangles; L339-343 runtime narrative borderline for reference. |
| `Lab file: container and its children/prose-review` | open | L74-78 35-word sentence on a colon, split into three; L84 "rather than a guess" is opinion; L86-87 reasons trailing rules; L139-145 mixes validation and lifecycle; L149-151 "machinery" jargon and shifting "it"; L171-174 "Only then does ..." inversion. |
| `Host configuration file/prose-review` | open | L24-25 "an error naming the missing line" vague (say the missing `import`); L44-45 passive "is reported"; L71 instruction "Set the same value on both hosts" in reference; L85 two thoughts in one bullet; L98 callout title carries no point ("`auto` never picks `sockmap`"); L99 "measured slower" with no number (code comment says ~8x, `fastpath/mod.rs:213`). |
| `Guest OS profile files/prose-review` | open | L14 "a directory in your config" vague, name it or drop since L21-23 does; L26-29 three rules in one sentence; L75 semicolon joining two definitions; L92-98 grab-bag paragraph (keyword sets, workspace floor, display downgrade), one thought each and put the downgrade beside the `display` row; L134-139 callout ends in an instruction, rephrase as a fact. |
| `wscript API: Lab and Segment/prose-review` | open | L26-28 two thoughts, split; L127 and L194 paraphrase errors, quote the real text (`mod.rs:500,527`); L128, L235, L299, L393 semicolons/colons joining thoughts; L347-348 "which" points at a clause and "gateways" as a verb; L443-449 60-plus-word sentence with parenthetical, split and quote the real NAT error (`labd/netservices.rs:199`); L438 "every host address" is IPv4-only (`runner.rs:312`); L303 and L450 bare "see" links. |
| `wscript API: Machine/prose-review` | open | L161-166 50-word sentence stacking "Unlike / so / such as / where", split; L255-257 three chained relative clauses; L349-350, L383 "canonical tree/copy" undefined at first use here (link `guide_dev` at first use); L382-384 "put back owing a re-seed" and L957-958 "The password crosses" are metaphors; L960-961 unclear "it"; L985-993 60-word sentence and "It fails, loudly," where the error text belongs; L1014-1015 "the pair `vmlab exec --user --password` takes" awkward; L407-410 passive "is asked for by name"; L143/L379 vs L299/L484 inconsistent placeholder quoting. |
| `wscript API: Term, types and the vmlab module/prose-review` | open | L12-13 noun string needs "that" twice; L38 mixes an error template into a sentence, put it after a colon; L61-63 and L140-141 "which" pointing at a whole clause; L79 and L158 semicolons; L158-159 ambiguous "it" and "VM" collides with the lab's VMs ("when the wscript runtime drops the handle"); L253 "cross resolved" internal jargon; L326-329 40-word sentence; L379-385 example attached to "schema change", give it its own sentence. |
| `vmlab vm/example-quality` | open | vm.wcl:20-36 subcommand table omits `help`, which `vmlab vm --help` lists (same omission in machine.wcl:19-26 and template.wcl:24-40; add a row or omit consistently). vm.wcl:92-93 quotes the no-address error without its machine-name prefix; actual is `<name>: no IPv4 address reported by agent` (src/cli/machine.rs:465). All 12 synopses, options, defaults and 3 examples otherwise match; exit codes verified. |
| `vmlab vm/cross-reference` | open | vm.wcl:150-154 names SSH multiplexer/alias, workspace syncer, sync ledger and agent-repair divergence with no links; link cmd_ssh_config, guide_dev or cmd_dev, cmd_machine. vm.wcl:132-133 "port forwards" not linked to guide_networking. |
| `vmlab vm/prose-review` | open | L11-15 one sentence carries scope, colon list and wscript clause; split. L66-68 "the host binaries are checked" passive with known actor; "vmlab checks the host binaries". L90-91 "it is the first NIC" has no referent; "Without `--nic`, the address is that of the first NIC in declaration order that holds one". L132-134 "when it gets a new lease" ambiguous (daemon or VM). L150-156 five passive deletions in one sentence; split into two active ones. L328-331 "an unreachable lab" applies to every subcommand, not only "the other" ones. |
| `vmlab machine/example-quality` | open | machine.wcl:176-179 says exit 1 for an agent "lacking the `metrics` feature"; no feature check exists (src/labd/mod.rs:795-799 calls `agent.stats(10s)`, src/labd/vm_agent.rs:695-716 only times out). Reword to "an agent that sends no metrics within 10 seconds". Table omits `help` (see vm). |
| `vmlab machine/cross-reference` | open | machine.wcl:148 callout names the "initramfs guest asset" unlinked; link guide_containers (or ref_paths). L124 relies on `vmlab status -v` showing `diverged=yes`; cmd_status does document it, so a link there suffices. |
| `vmlab machine/prose-review` | open | L11 "a machine, VM or container, what it can do" reads as a three-item list; "whether VM or container". L67-69 "reads differently from a feature list that lacks something" vague; "`-` means no answer, not an empty feature set". L92-94 passive; "vmlab renders sizes in binary units; when the guest reports no total, the line shows only the used figure". L118-127 one paragraph mixes verb behaviour, template guarantee and rebuild remedy; split after "records the machine as **diverged**." L126-127 quoted slogan repeats L122; cut. L148 callout first sentence is 33 words with two subordinate clauses; shorten. |
| `vmlab template/example-quality` | open | (1) template.wcl:53, 97-98, 109 claim `invalid_argument` (2) for a name declared for several architectures with none chosen; unreachable: src/template/cli.rs:403-418 iterates every declared (arch, name) and always sends `arch: Some(..)`, so an ambiguous name builds each architecture in turn. Drop the code, say every architecture builds. (2) L181 search `--registry` default "the vmlab registry"; src/template/registries.rs:56-69 and src/supervisor/store.rs:453-466 seed three namespaces (ghcr.io/vmlabdev/vmlab-templates, ghcr.io/vmlabdev, docker.io/library) filtered by `--kind`; say so (the --help text is also stale, report upstream). (3) L135-137 stop: fallback `store.stop_push` (cli.rs:457-466, templates.rs:118-128) can also answer `not_found` (4) and `conflict` (5); say "not_found when nothing is running, conflict possible in a race, otherwise failed". (4) L103-104 example `cd examples/templates && vmlab template build win2022` fails: examples/templates/ has no vmlab.wcl; real path is examples/templates/windows-server-2025 with template name `windows-server-2025`. L105's `templates.wcl`/`alpine` exist nowhere; use a real file or mark illustrative. (5) L68 build `-f` default matches code (cli.rs:371 walks up); --help says `./vmlab.wcl`, which is the stale side; no chapter change. |
| `vmlab template/cross-reference` | open | L85-86 names `template.op.log`/done/cancelled/error events without linking ref_events; L361-362 names `vmlab up`/`vmlab pull` without cmd_up/cmd_pull. |
| `vmlab template/mode-fit` | open | L77-78 "because the supervisor does not stand where you do" and L386-388 "on a shared machine pass a token..." are second-person asides; reword to "the supervisor's working directory is not the shell's" and "A token with limited scope is safer than an account password on a shared machine." |
| `vmlab template/prose-review` | open | L12-13 "moving templates through archives" vague; "exporting and importing archives". L19 "so it can be stopped from another terminal" name the actor. Hidden-actor passives at L219, L302-303, L360-361 ("is refused"); "the supervisor refuses ... unless you pass `--force`". L56-57 "Each subcommand below names the codes it can produce" is filler; cut. L153 "The table has columns" → "The table's columns are". L246-247 and L250 paraphrase output the chapter elsewhere quotes verbatim; quote `removed N build(s), freed X` and `nothing to clean — every template is within --keep N`. L73-80 and L123-131 are single clause-chains; split L124-128. |
| `vmlab console/example-quality` | open | console.wcl:30-31 quotes the refusal as `lab "<name>" is not running`; actual (src/cli/console.rs:29) appends ` — \`vmlab up\` first`. L49-52 omits the second line printed under `--tcp`: `connect a VNC viewer there (Ctrl-C to stop the bridge)` (console.rs:46). Exit paragraph correct. |
| `vmlab script/example-quality` | open | script.wcl:52-53 says a failing call ends with `failed` (1); src/labd/mod.rs:437-441 uses `?` and `CommandError::from(anyhow::Error)` (src/proto/error.rs:192-197) recovers a nested CommandError, so a call that hits a missing machine can surface `not_found` (4) or `conflict` (5). Say "the code the failing call carried, `failed` (1) when it carried none". |
| `vmlab dev/example-quality` | open | dev.wcl:41-48 chapter-level exit paragraph says the sync verbs return `not_found` (4) for a machine the daemon does not know; `dev sync status` fails client-side with anyhow (src/cli/dev.rs:280, exit 1) and the daemon wraps every WorkspaceFlush/Resolve/Diff error as `CommandError::failed` (src/labd/mod.rs:958-979). Only `attach`'s capabilities probe yields 4. The per-verb paragraphs at L217/242/280/319 are already right; fix the chapter-level one to match. |
| `vmlab dev/progressive-disclosure` | open | L44 and L182 use "status projection", defined nowhere in Parts I–III; say "the status `vmlab status` and the console show" and link cmd_status. |
| `vmlab dev/cross-reference` | open | L199, L233 name snapshot capture refusing/flushing without linking guide_snapshots or cmd_snapshot; L236 `vmlab status` unlinked; L182 "the console" unlinked. |
| `vmlab dev/prose-review` | open | L62 "`attach` is cold to editing in one command" names a slogan; say what it does. L119 "so it cannot say this" vague referent. L153 drop "necessarily". L163-164 "in order to" → "to". L258-260 "rather than attaching and looking" hard to parse; "attaching and opening the file shows only the guest side". L299-300 "and the two conflict" redundant. L83-88 five printed items in one sentence; split. L34-37 rewrite as "If a rung names a machine that is not a dev machine in this lab, the error says which rung named it and lists the candidates." |
| `vmlab ssh/example-quality` | open | ssh.wcl:74 "a lab or machine that is not declared" exits 1; for the qualified form the message is `lab "x" is not running` (src/cli/lab.rs:1058-1060), a registry lookup, not a declaration check; wording only. L60-61 covers `ssh-proxy`; one clause that it takes `[lab/]machine` and fails at once rather than waiting (lab.rs:1243-1257) would help an editor-log reader. |
| `vmlab cp/example-quality` | open | cp.wcl:64-68 omits that a bare `<vm>` with no daemon starts the lab daemon (src/cli/lab.rs:68-71 `ensure_lab_daemon`), unlike ssh/exec, and that the `lab/name` form on a stopped lab exits 1 with `lab "x" is not running` (lab.rs:63-65). |
| `vmlab container/example-quality` | open | container.wcl:234-235 "every subcommand exits 1 on a container that is not running" overclaims: stop/destroy on a stopped container return Ok (src/labd/container.rs:912), start starts it, logs reads the host file (chapter says so at L171-172). Only exec, ip and shell fail. Everything else verified. |
| `vmlab container/prose-review` | open | L54-56 passive chain; "If the image is still pending, the daemon pulls it first with progress on the terminal. It then checks the host binaries and boots the micro-VM." L137-139 "are mirrored ... once it finishes" → "When the command finishes, the verb prints its stdout and stderr and exits with its exit code." L149-150 missing comma before "naming". L168-171 three steps in one sentence; split. L232-233 "Exit 6 is what `logs` answers" → "`logs` exits 6 (`unsupported`) on a machine with no console log". |
| `vmlab snapshot/cross-reference` | open | snapshot.wcl:115 "the event feed" unlinked; link guide_events. |
| `vmlab snapshot/prose-review` | open | L13-17 one sentence with colon and semicolon carries both snapshot kinds; three sentences. L60-62 passive and hard to parse; "The lab's state records each snapshot: whether it was online, when it was taken and, for a container, the image digest." L71, L109, L119 bold for emphasis; drop. L72-74 "not a thing to take against a warning" colloquial. L76 "passes straight through" vague; "A machine with no workspace is snapshotted directly." L97-100 and L105-108 dense; split each into two sentences. L113 "rides the ledger" → "is stored in the ledger". |
| `vmlab lab/example-quality` | open | lab.wcl:171-177 example captioned "Pick up an edited lab file without re-provisioning" runs `lab stop`, `lab restart`, `up`; `lab stop` sends the same Down as `vmlab down` (src/cli/lab.rs:724-730) and `up` re-runs every provision unconditionally (src/labd/lab.rs:1439-1484), which L112-114 itself says. Drop "without re-provisioning". |
| `vmlab lab/prose-review` | open | L32-34 "read-only subcommands ... with none running" ambiguous; "`list` and `info` never start the supervisor. With no supervisor running, the registry is empty and `list` says so." L69-75 40-word sentence with nested example; split. L124-126 "is not a veto" jargon; "does not block the restart". L129-132 dangling modifier and passive; "Run inside the lab's directory, it asks the supervisor to restart from that directory; run elsewhere, it uses the registry's entry." L112 colon connector; two sentences. |
| `vmlab playbook/example-quality` | open | playbook.wcl:29 says only check/apply start the lab daemon; `list` goes through `lab_client_for(None)` → `ensure_lab_daemon` too (src/cli/lab.rs:1622); say so. L100-101 "waits for it to come back, and resumes": the daemon re-runs the same apply from the start (src/labd/playbook.rs:668-717; guide L113-116 agrees); "runs the apply again". |
| `vmlab playbook/prose-review` | open | L46-50 three output formats in one sentence; one per line kind. L67-69 colon connector plus three clauses; "Pushes the playbook folder into the guest again, so the host's current copy is what runs. Then it runs config-weave in check mode, which reports drifted steps and changes nothing." L76 passive "Which block runs is resolved from". L103-104 overlong source line. L133-142 exit 2 is both "failed validation" and "usage error"; say the two are indistinguishable by code. |
| `vmlab clipboard/example-quality` | open | clipboard.wcl:31-32 and 92-93 say the agent must advertise the `clipboard` feature and exits 1 otherwise; the daemon checks no feature (src/labd/mod.rs:809-818) and `set` is fire-and-forget (src/labd/vm_agent.rs:754-756), printing `copied N bytes` and exiting 0 on an agent that ignores it; only `get` fails, by 10 s timeout (vm_agent.rs:762-764). Scope the precondition to `get` and say `set` cannot detect an ignoring agent. |
| `vmlab dns/example-quality` | open | dns.wcl:56-63 example table shows only `<machine>.<lab>.<suffix>` rows; the zone also registers bare `<machine>.<suffix>` (src/labd/network.rs:363-364), so a real table has `dc01.vmlab.internal` rows too; add them or say the example is trimmed. |
| `vmlab dns/prose-review` | open | L30-31 comma apposition reads as a three-item list; "Global segments and segments with `dns { enabled = false }` have no local zone, so they serve nothing and are not listed." L41-43 "a sinkhole answers with nothing and `NXDOMAIN` and `0.0.0.0` fail differently" contradicts itself (a 0.0.0.0 sinkhole answers with an address); "a sinkhole has no address worth showing; the mode is what matters because NXDOMAIN and 0.0.0.0 fail differently in a guest". |
| `vmlab up/callout-admission` | open | up.wcl:75-78 "A failed first-boot leaves the machine running" is `kind = :note` but is a neutral behaviour fact, not background to remember later; `:info`. Content verified (src/labd/lab.rs:1543, 1800 s). |
| `vmlab pull/example-quality` | open | pull.wcl:44 quotes one Ctrl-C message; src/cli/lab.rs:147-150 has a second, `interrupted — no download was in flight`. Add it. |
| `vmlab pull/cross-reference` | open | pull.wcl:11 links "template" to guide_registries where up.wcl:13 links the same word to guide_templates; link "registry template" as a phrase to guide_registries, or template to guide_templates. L51 `vmlab template build` unlinked to cmd_template. |
| `vmlab destroy/cross-reference` | open | destroy.wcl:51 warning callout names `vmlab dev sync flush` with no link to cmd_dev while the surrounding prose links cmd_machine, cmd_template, cmd_lab. |
| `vmlab status/prose-review` | open | status.wcl:96 first example is a bare code block with no lead sentence, unlike the second at L111 and every sibling; add "Print the lab's tables:". Columns, seven STATUS labels and verbose fields verified against src/cli/lab.rs:327-509 and src/status.rs:127-138. |
| `vmlab fastpath/example-quality` | open | fastpath.wcl:54-58 exit paragraph omits the usage-error exit 2 every sibling states; could also add that a malformed `VMLAB_FASTPATH` is ignored with a warning (src/net/fastpath/mod.rs:176-200). Mode semantics at L32-44 verified. |
| `vmlab exec/example-quality` | open | exec.wcl:65-69 omits (a) that the guest exit code is passed through verbatim via `std::process::exit` (src/cli/lab.rs:1590-1593), so a guest exiting 4/5/6 collides with vmlab's own codes, and (b) that a lab not running or no lab in the cwd exits 1 via `lab_client_for` (lab.rs:60-65) and a malformed `lab/` ref bails (lab.rs:55). The same lab-not-running omission is in shell.wcl:60-63, tail.wcl:50-53, eventlog.wcl:50-54, osinfo.wcl:40-43. |
| `vmlab exec/cross-reference` | open | exec.wcl:14-15 names `vmlab container exec` unlinked; link cmd_container. |
| `vmlab exec/prose-review` | open | L43-45 "starts failing where it used to work" and "is the old behaviour, spelled out" carry --help's release-note framing; rewrite as present-tense fact. L53 overlong source line. |
| `vmlab shell/cross-reference` | open | shell.wcl:14-15 names `vmlab container shell` unlinked; link cmd_container. |
| `vmlab shell/prose-review` | open | L41-44 restates the `--user` table row (L24) and the exec chapter; keep only the pointer to cmd_exec. |
| `vmlab eventlog/progressive-disclosure` | open | eventlog.wcl:37-38 "the agent's negotiated features" used without introduction; link cmd_machine (`machine capabilities`) or say "the features the guest's agent advertises". |
| `vmlab logs/prose-review` | open | logs.wcl:45-46 "polls the first matching file" is accurate (src/cli/lab.rs:2036-2040) but buries the consequence: for a `lab/vm` target only qemu.log is followed, never serial.log. State it directly. |
| `Troubleshooting/example-quality` | open | `docs/manual/pages/appendices/troubleshooting.wcl:334-335` says the per-lab daemon's log is `.vmlab/lab.log` inside the lab directory. The daemon's stdout/stderr goes to `$XDG_STATE_HOME/vmlab/labd-<lab>.log` (`src/supervisor/mod.rs:253`) and `lab.log` is provision output under `state/labs/<lab>/` (`src/labd/mod.rs:216-219`), which is what `vmlab logs` reads. Fix the sentence to name both state-dir paths. Also `:40` and `:357` give `vmlab lab restart` with no argument; the verb requires `<LAB>` (`vmlab lab restart --help`). `:188` quotes the conflict-halt headline as the only halt message, but a pure bulk-delete halt prints `the workspace on "{machine}" has stopped: {bulk}` (`src/labd/workspace/halt.rs:140`); say so in the bulk-delete paragraph at `:219-223`. `:256` shows `{machine}: {forward}` unquoted, but the three table reasons at `:270-272` render as `"{machine}": {forward}: {why}` (`src/labd/forward_plan.rs:150,210,223`); add the quotes or note the difference. Every other quoted message exists verbatim in `src/`. |
| `Troubleshooting/cross-reference` | open | `:41`, `:175`, `:294`, `:308` link to `apx_troubleshooting` itself, which lands the reader at the top of the same page; drop the links or say "below"/"above". "wave" (`:129`) and "ledger" (`:251`) are unlinked concepts; link `guide_architecture` and `guide_dev`. |
| `Troubleshooting/prose-review` | open | `:195` misattributes the full path listing to the watch staying up; it comes from scan-then-halt. `:134-138`, `:157-163`, `:203-212`, `:219-229` are semicolon chains carrying four or more items each; turn them into bullets or a `Verb | Does` table. `:128-131`, `:261-264`, `:275-277` are 40-word sentences with a passive and a clause-pointing "which". `:98` "or the pinned version is not" drops its complement; `:107-110` "A store ref ... is a different message". `:282-284` "is terminated on the host" reads two ways. `:318-321` links "no tap" to the silent degrade as a non sequitur, then splices in the VNC point. `:299-300` "it is not a credential" has two referents. `:351`, `:356-358` clause-pointing "This"/"which"; `:382` "As it says." is filler. `:361` heading says "eBPF fast path" where the rest of the manual says "fast path". |
| `Glossary/example-quality` | open | `glossary.wcl:36` "Logs to `.vmlab/lab.log`" is wrong; the daemon logs to `labd-<lab>.log` under the state root (`src/supervisor/mod.rs:253`). `:33` names the host config `vmlab-host.wcl`; the file is `config.wcl` under the config root (`src/config/host.rs:72`) and `vmlab-host.wcl` is the schema it imports. Terms the appendices lean on with no row: "guest asset", "ready", "wave", "diverged", "TCG". |
| `Glossary/cross-reference` | open | `:46`, `:48` use "rung" from the PRD's login ladder with no entry and no link to where `guide_logins` explains it; `:39` "a surface attaches as" names nothing the manual defines. Every other term links to the chapter that defines it (re-seed to `guide_snapshots`, wscripti to `ref_ws_lab` both confirmed). |
| `Glossary/prose-review` | open | `:37` ledger row is dense, passive and metaphoric ("ride it"); `:51` re-seed is a 45-word sentence; `:23`, `:29`, `:31` are verbless or passive fragments ("a warning at `up`, a refusal at attach", "Planned as a whole", "nothing is written"); `:24` ambiguous subject of "syncs"; `:67` depends on the previous row; `:16` the promised login/logon and snapshot/workspace contrasts are not carried into the `:55` snapshot row; `:22` "shipped with the host for containers" is unclear; `:40` "a real session" adds nothing. |
| `Example labs/example-quality` | open | `examples.wcl:182` `vmlab ssh-config --print` takes a required `<MACHINE>` value; write `vmlab ssh-config --print dev01`. `:328-331` "Seven are Linux cloud images with a `cloudinit/` folder" counts Ubuntu 24.04, which the next sentence says is an ISO autoinstall; say six cloud images plus one ISO. All ten directories, all `source_file` paths (resolved against `main.wcl`), the other run commands, ports, IPs, template names and versions check out. |
| `Example labs/progressive-disclosure` | open | `:186` "two riders §19.8 left open" is a PRD section number and PRD vocabulary a manual reader has not met; say "two open questions the README records". |
| `Example labs/prose-review` | open | `:32-39` one 60-word sentence; slashes at `:27-28`, `:127`, `:139`, `:193-194`; `:369-370` "and" groups two ways; `:253-255` "the trunk table converges the mutual dial"; `:223-227` filler lead-in then 35 words; "NAT'd" at `:36`, `:63`, `:213`, `:302`, `:378`; hedged "should print" at `:68`, `:305` in a reference; `:168` "the harder half" is opinion; "land/landing" does five jobs (`:124`, `:136`, `:175`, `:185`, `:334`); serial comma missing in every "Illustrates" line. |
| `Events/example-quality` | open | `events.wcl:60` puts the first-boot-seal case under `vm.ready` and says "for a container ... the key is `container`", but the name is built as `{subject}.ready` (`src/labd/lab.rs:1549-1552`), so a container emits `container.ready`, not `vm.ready` with a `container` key. Move that sentence to the `container.ready` row at `:65`, which currently omits the seal case. Every other name and payload matches the emit sites in `src/labd` and `src/supervisor`; nothing is missing or extra. |
| `Events/prose-review` | open | `:20-22` four fields in one semicolon sentence; `:23-27` 45 words with "folds into a host-wide aggregate"; one thing, three names across `:165` "post-restore re-convergence", `:177` "re-seed", `:178` `reconverged`; `:96` field grouping ambiguous; dropped verbs at `:52-54`, `:72`; `:60` passive "was sealed"; `:46-47` passives and a 48-word cell; `:166` ambiguous "it"; `:172` ungrammatical parenthetical; `:45` "on the way through `destroy`"; `:32-33` "natural subject", `:161` "lands owned". |
| `Files and directories/example-quality` | open | `paths.wcl:39-52` Data table omits `labs/`, the managed home the web UI creates new labs in (`src/paths.rs:50-52`). `:51` lists `~/.local/share/config-weave/bin` as if under the data root; it is `$XDG_DATA_HOME/config-weave/bin`, a sibling (`src/paths.rs:56-58`). Everything else (four roots, empty-means-unset, `HOME` to `/`, `/tmp/vmlab-<uid>` fallback and 0700, `VMLAB_WORK_DIR` with the 12-hex hash) matches `src/paths.rs`; every listed env var is read in `src/`. |
| `Files and directories/prose-review` | open | `:47` second clause has no verb and the "when" attaches to both; `:117-120` 45-word sentence; `:31-34` two thoughts, passive; `:35-36` "some deployments"; `:82` "moves it" has two referents; `:121-122` clause-pointing "This"; `:81`, `:132` passive without actor; `:100-101` "should be" in reference; `:70-71` semicolon; `:15` "materialises". |
| `Wire protocol and error codes/example-quality` | open | `protocol.wcl:103` "Every lab-daemon command is called by the CLI" contradicts `docs/protocol.md:62,108`, where `ping` and `shutdown` are `cli`, `daemon`. `:67` says a CLI-side failure "exits non-zero"; it exits 1 (`src/cli/mod.rs:830`). The error-code table and both command lists otherwise match `docs/protocol.md` exactly (7 codes, 25 supervisor commands, 47 lab-daemon commands). |
| `Wire protocol and error codes/cross-reference` | open | `:53` "[Commands](cmd_up)" links the whole part to the `vmlab up` chapter; there is no part page, so either say "each chapter in Commands" without a link or link the specific chapters. |
| `Wire protocol and error codes/prose-review` | open | `:13-15` 35 words, passive, "the reason each command is reachable from where it is"; `:43-46` "surface", passive, names no file; `:102-107` 45-word sentence ending in a clause-pointing "which"; `:22-24` comma splice around the link; `:29-32` two thoughts; `:113-114` "adopted unchanged" misreads; `:77` "and daemons for `ping` and `shutdown`" groups two ways; `:42` semicolon; `:12-13` "out-of-repo"; `:64` passive; `:88` repeats `:72` verbatim. |
