E0260  1:20-1:46
  non-exhaustive match: pattern `_` is not covered
  help: add an arm matching `_`, or a trailing `_ => ...` arm; note that arms with `if` guards never count toward exhaustiveness (matching on `int`)

