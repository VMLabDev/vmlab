fn main() {
    let arith = 1 + 2 * 3 - 4 / 5 % 6
    let cmp = 1 + 2 < 3 * 4
    let eq = 1 < 2 == 3 > 4
    let logic = a && b || c && d
    let mixed = 1 + 2 < 3 && 4 * 5 > 6 || x
    let unary = -a + -b
    let not = !x && !y
}
