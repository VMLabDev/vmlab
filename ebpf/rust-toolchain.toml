# Pinned so the committed BPF objects are byte-reproducible: bpf-linker links
# against this toolchain's libLLVM, so the channel pin (not just "nightly")
# is load-bearing. Keep in sync with .github/workflows/ci.yml (ebpf-verify).
[toolchain]
channel = "nightly-2026-05-22"
components = ["rust-src"]
